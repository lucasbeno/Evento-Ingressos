# Só necessário em produção (build). Em desenvolvimento, o Vite já faz proxy
# de /api para o backend local (ver vite.config.ts) — não precisa disso.
VITE_API_URL=https://sua-api-em-producao.exemplo.com
